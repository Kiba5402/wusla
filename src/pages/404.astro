---
import { Image } from 'astro:assets';
import Layout from '~/layouts/Layout.astro';
import { getHomePermalink } from '~/utils/permalinks';
import baseLogo from '../assets/favicons/icon2.png';

const title = `Error 404`;
---

<Layout metadata={{ title }}>
  <section class="flex items-center h-full p-16">
    <div class="container flex flex-col items-center justify-center px-5 mx-auto my-8">
      <div class="max-w-md text-center">
        <h2 class="mb-8 font-bold text-9xl">
          <span class="sr-only">Error</span>
          <span class="text-primary">404</span>
        </h2>
        <p class="text-3xl font-semibold md:text-3xl">Lo sentimos, no hemos podido encontrar esta página.</p>
        <p class="mt-4 mb-8 text-lg text-muted dark:text-slate-400">
          Pero no te preocupes, puedes encontrar muchas otras cosas en nuestra página de inicio.
        </p>
        <a rel="noopener noreferrer" href={getHomePermalink()} class="btn ml-4">
          Volver a la página de inicio
          <Image
            src={baseLogo}
            alt="Logo Wusla"
            width="180"
            class="w-7 h-5 mx-1 float-right rounded-sm"
          />
        </a>
      </div>
    </div>
  </section>
</Layout>
